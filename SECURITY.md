# セキュリティポリシー / Security Policy

## 対応バージョン

`main` ブランチの最新コミットのみ対応しています。

## 脆弱性の報告

脆弱性を見つけた場合は、Issue を立てずに以下へ報告してください。

- GitHub のプライベート脆弱性レポート:
  https://github.com/woolisbest-honke/proxy/security/advisories/new

可能な範囲で、再現手順・影響・回避策を添えていただけると助かります。

## 設計上のセキュリティ特性

- **オープンプロキシではありません。** 転送先は環境変数 `TARGET_URL` の 1 つに固定されており、
  利用者からの入力で任意のホストへ転送することはできません（SSRF の悪用経路にならない設計）。
- **転送先の TLS 証明書を既定で検証します**（`TLS_VERIFY=false` にしない限り）。
- **クライアントの IP を既定で転送先へ通知しません**（`X-Forwarded-For` 等を付与しない。
  `FORWARD_CLIENT_IP=true` で明示的に有効化する必要がある）。
- hop-by-hop ヘッダー（`Connection`, `TE`, `Transfer-Encoding` 等）は双方向で除去されます。
- リクエスト・レスポンスのボディは内容を解釈・書き換えせず、ストリームのまま転送します。

## 運用上の推奨事項

1. **オリジン側のアクセス制限。** 元サイトの IP を本当に隠すには、オリジンのファイアウォール /
   WAF で「プロキシが動くプラットフォームの送信 IP のみ」を許可してください。
   拒っておかないと、DNS 履歴や従来のレコードから元 IP が判明する可能性があります。
2. **HTTPS を強制する。** ほとんどの PaaS は `https` を終端します。オリジンとの通信は
   必ず `TARGET_URL` を `https://` にして平文を残さないでください。
3. **アクセスログに注意。** `LOG=true` の場合、URL（クエリ文字列を含む）がログに出力されます。
   クエリにトークン等が含まれる API を転送する場合は、ログの保管・転送先に注意するか `LOG=false`
   にしてください。プロバイダーのログも同様です。
4. **依存関係の更新。** `npm audit` を定期的に実行し、Dependabot（`.github/dependabot.yml` 有効）
   の PR を確認してください。
5. **レートリミット。** このプロキシ自体には認証・制限機能がありません。一般公開する場合は
   前段に Cloudflare 等を置いてレートリミットやボット対策を行うことを推奨します。
6. **`TLS_VERIFY=false` は自己署名証明書のオリジンでのみ使う。** `false` にすると中間者攻撃の
   検知が無効になります。

## 既知の制限

- HTML / JS 内に埋め込まれた転送先ドメインの絶対 URL は書き換えられません。該当部分への
  アクセスはオリジンへ直接行われます。
- 転送先以外のドメインへのリダイレクト（`Location`）は検証せずそのまま通します。
- Websocket（`ws://` / `wss://`）には対応していません（仕様: HTTP / HTTPS のみ）。
